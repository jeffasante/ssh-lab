services:
  server:
    build: ./server
    ports:
      - "8081:8080"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3002:80"
    depends_on:
      - server
    restart: unless-stopped
